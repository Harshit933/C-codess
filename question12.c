#include<stdio.h>

int main(){
    int a =9, b =10;
    if(a & b){
        printf("Bitwise AND operation is performed");
    }
    if(a | b){
        printf("Bitwise OR operation is performed");
    }
    return 0;
}